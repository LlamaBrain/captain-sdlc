# captain-sdlc is a documentation repo, not a Unity project — never track .meta
# (Unity only stamped these when the docs lived inside the ATH package; see TD-001.)
*.meta

# Local tooling state
.claude/
.tmp/

# Work-in-flight register — kept local, not published. The design set (ADRs,
# roadmap, vision, seams, glossary) is the public face; scratch.md is the
# intraday handoff register and stays out of the public tree (OSS boundary
# decision, 2026-05-29). The /scratch skill still maintains it locally.
scratch.md

# OS cruft
.DS_Store
Thumbs.db

# .NET build outputs
bin/
obj/
